def operations(a, b, c):
    if c == '+':
        return a+b
    if c == '-':
        return a-b
    if c == '*':
        return a*b


def list(index):
    for i in range(1,10):
        if i == index:
            return i
def myfunc(a_list, idx):
    return a_list[idx]
